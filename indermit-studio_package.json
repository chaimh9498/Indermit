{
  "name": "indermit-studio",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "worker"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:api": "npm run dev --workspace worker",
    "build": "npm run build --workspace frontend",
    "test": "npm run test --workspace worker",
    "check": "npm run build && npm run test"
  },
  "engines": {
    "node": ">=20"
  }
}
